name: mmu-admin
repo_url: https://github.com/Software-Mathematics/MMUAdminUI.git
run_command:
src_path: MMU_AdminUI
application_port: 80
deploy_port: 9130
deploy_servers:
  - 161.97.136.16
build_command: . /root/.nvm/nvm.sh && nvm install 16 && nvm use 16 && node -v && npm install && npm run build
deploy_env:
  - dev
  - prod
